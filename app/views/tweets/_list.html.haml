- tweets.each do |tweet|
  .panel.panel-default
    .panel-heading
      %h4.panel-title
        = link_to tweet.user.email, user_path(tweet.user.id)
        %small= "tweeted #{time_ago_in_words(tweet.created_at)} ago"
    .panel-body
      = tweet.text
